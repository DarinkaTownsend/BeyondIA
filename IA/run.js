const { main } = require('./puntaje_total.js');

// Define los valores de IDCONSULTOR e IDPROYECTO
const IDCONSULTOR = 1;
const IDPROYECTO = 1;

// Llama a la función main con los parámetros
main(IDCONSULTOR, IDPROYECTO);